spring:
  datasource:
    driver-class-name: net.sf.log4jdbc.sql.jdbcapi.DriverSpy
    url: jdbc:log4jdbc:mysql://3.39.123.2:3306/admin-board?serverTimezone=UTC&allowPublicKeyRetrieval=true&useSSL=false&allowMultiQueries=true&autoreconnect=true
    username: root
    password: hyang461512@
  banner:
    location: "classpath:banner.txt" 
    charset: UTF-8
  servlet:
    multipart:
      enabled: true
  mvc:
    pathmatch:
      matching-strategy: ant-path-matcher
                    
mybatis:
  mapper-locations: mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true

server:
  port: 2000
  
properties:
  aesKey: zeedeco1234!@#$%
   
file:
  upload:
    location: /data
    
